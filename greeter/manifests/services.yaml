apiVersion: v1
kind: Service
metadata:
  name: greeter-svc
  namespace: exercises
spec:
  ports:
  - port: 80
    targetPort: 3000
    name: http
  selector:
    app: greeter
---
apiVersion: v1
kind: Service
metadata:
  name: greeter-svc-1
  namespace: exercises
spec:
  ports:
  - port: 80
    targetPort: 3000
    name: http
  selector:
    app: greeter
    version: v1
---
apiVersion: v1
kind: Service
metadata:
  name: greeter-svc-2
  namespace: exercises
spec:
  ports:
  - port: 80
    targetPort: 3000
    name: http
  selector:
    app: greeter
    version: v2
