apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: greeter-split
  namespace: exercises
spec:
  parentRefs:
  - name: greeter-svc
    kind: Service
    group: ""
    port: 80
  rules:
  - backendRefs:
    - name: greeter-svc-1
      port: 80
      weight: 75
    - name: greeter-svc-2
      port: 80
      weight: 25
